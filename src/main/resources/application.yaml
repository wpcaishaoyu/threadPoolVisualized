server:
  port: 9092
management:
  server:
    port: 9092
  endpoints:
    web:
      exposure:
        include: '*'
      base-path: /management
spring:
  application:
    name: springboot_prometheus
logging:
  level:
    root: debug